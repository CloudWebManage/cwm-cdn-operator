---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: cdntenants.cdn.cloudwm-cdn.com
spec:
  group: cdn.cloudwm-cdn.com
  names:
    kind: CdnTenant
    listKind: CdnTenantList
    plural: cdntenants
    singular: cdntenant
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: CdnTenant is the Schema for the cdntenants API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of CdnTenant
            properties:
              config:
                additionalProperties:
                  type: string
                type: object
              domains:
                items:
                  properties:
                    cert:
                      type: string
                    config:
                      additionalProperties:
                        type: string
                      type: object
                    key:
                      type: string
                    name:
                      type: string
                    tls:
                      properties:
                        maxVersion:
                          enum:
                          - TLSv1.2
                          - TLSv1.3
                          type: string
                        minVersion:
                          enum:
                          - TLSv1.2
                          - TLSv1.3
                          type: string
                        mode:
                          enum:
                          - provided
                          - letsencrypt
                          type: string
                        redirectHttpToHttps:
                          type: boolean
                      type: object
                      x-kubernetes-validations:
                      - message: minVersion cannot be greater than maxVersion
                        rule: '!(has(self.minVersion) && has(self.maxVersion) && self.minVersion
                          == ''TLSv1.3'' && self.maxVersion == ''TLSv1.2'')'
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
              elasticsearch:
                description: Elasticsearch configuration for sending access logs
                properties:
                  config:
                    additionalProperties:
                      type: string
                    description: Configuration Options
                    type: object
                  enabled:
                    description: Enable or disable Elasticsearch logging
                    type: boolean
                required:
                - config
                - enabled
                type: object
              origins:
                items:
                  properties:
                    config:
                      additionalProperties:
                        type: string
                      type: object
                    healthCheck:
                      properties:
                        enabled:
                          description: Enabled controls active origin health checks.
                            Defaults to true when omitted.
                          type: boolean
                        expectedStatus:
                          description: ExpectedStatus is the HTTP status considered
                            healthy. Defaults to 200 when omitted.
                          format: int32
                          maximum: 599
                          minimum: 100
                          type: integer
                        healthyThreshold:
                          description: HealthyThreshold is the consecutive success
                            count required to mark healthy. Defaults to 2 when omitted.
                          format: int32
                          minimum: 1
                          type: integer
                        interval:
                          description: Interval is the active health check interval,
                            e.g. 10s. Defaults to 10s when omitted.
                          type: string
                        path:
                          description: Path is requested for active origin health
                            checks. Defaults to / when omitted.
                          maxLength: 2048
                          pattern: ^/.*
                          type: string
                        timeout:
                          description: Timeout is the active health check timeout,
                            e.g. 2s. Defaults to 2s when omitted.
                          type: string
                        unhealthyThreshold:
                          description: UnhealthyThreshold is the consecutive failure
                            count required to mark unhealthy. Defaults to 3 when omitted.
                          format: int32
                          minimum: 1
                          type: integer
                      type: object
                    name:
                      description: Name identifies this origin in generated config,
                        logs, and health output.
                      maxLength: 63
                      type: string
                    url:
                      maxLength: 2048
                      type: string
                    weight:
                      description: Weight controls load-balancing preference. Defaults
                        to 1 when omitted.
                      format: int32
                      minimum: 1
                      type: integer
                  required:
                  - url
                  type: object
                maxItems: 32
                minItems: 1
                type: array
                x-kubernetes-validations:
                - message: origin URLs must use http or https scheme and include a
                    host
                  rule: self.all(o, o.url.matches('^https?://[^/?#]+($|[/?#])'))
                - message: path-prefixed origin URLs are not supported with multiple
                    origins
                  rule: self.size() == 1 || self.all(o, !(o.url.matches('^https?://[^/]+/.+')))
            required:
            - domains
            - origins
            type: object
          status:
            description: status defines the observed state of CdnTenant
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the CdnTenant resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              domainTLS:
                items:
                  properties:
                    message:
                      type: string
                    mode:
                      type: string
                    name:
                      type: string
                    ready:
                      type: boolean
                    reason:
                      type: string
                  required:
                  - message
                  - mode
                  - name
                  - ready
                  - reason
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
